{% macro TwitterCard(data) -%}

  {# REQUIRED #}
  {%- if data.twitterCard -%}
    <meta name="twitter:card" content="{{ data.twitterCard }}">
  {% else %}
    <meta name="twitter:card" content="summary">
  {%- endif -%}

  {# The Twitter @username the card should be attributed to #}
  {%- if data.twitterSite -%}
    <meta name="twitter:site" content="{{ data.twitterSite }}">
  {%- endif -%}

  {# REQUIRED #}
  {% if data.twitterTitle -%}
    <meta name="twitter:title" content="{{ data.twitterTitle }}">
  {%- endif -%}

  {% if data.twitterDescription -%}
    <meta name="twitter:description" content="{{ data.twitterDescription }}">
  {%- endif -%}

  {%- if data.twitterImage -%}
    <meta name="twitter:image" content="{{ data.twitterImage }}">
  {%- endif -%}

  {%- if data.twitterImage and data.twitterImageAlt -%}
    <meta name="twitter:image:alt" content="{{ data.twitterImageAlt }}">
  {%- endif -%}

{%- endmacro %}

{{ TwitterCard({
  "twitterCard": "Page title",
  "twitterSite": "@username",
  "twitterTitle": "Page title",
  "twitterDescription": "Page description",
  "twitterImage": "https://",
  "twitterImageAlt": ""
}) }}
